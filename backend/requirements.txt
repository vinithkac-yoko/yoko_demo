# Backend runtime deps. The engine is imported from ../engine
# (install with `pip install -e ./engine`, or via the root requirements.txt).
fastapi>=0.110
uvicorn[standard]>=0.29
pydantic>=2
anthropic>=0.69          # VLA agent loop (Messages API tool use + vision)
cairosvg>=2.7            # optional raster render for vision; needs libcairo at runtime
